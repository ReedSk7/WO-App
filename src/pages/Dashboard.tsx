import { loadDrafts } from '../storage/local';
import { sampleCRs } from '../data/sampleCRs';
export default function Dashboard(){const drafts=loadDrafts(); const needs=drafts.filter(d=>d.status==='Needs Info').length; const ready=drafts.filter(d=>d.status==='Review Ready').length;
return <div className='space-y-4'><div className='card text-sm font-medium'>This demo creates draft planning content only. It is not approved work direction. Qualified planner review, approved procedures, verified technical data, and site-required reviews are required before use.</div><div className='grid md:grid-cols-4 gap-3'>{[['Demo CRs',sampleCRs.length],['Saved Drafts',drafts.length],['Incomplete Drafts',needs],['Review-Ready Drafts',ready]].map(([t,v])=><div key={String(t)} className='card'><div>{t}</div><div className='text-2xl font-bold'>{v}</div></div>)}</div><div className='card'><h2 className='font-semibold mb-2'>Recent Drafts</h2>{drafts.slice(0,5).map(d=><div key={d.id} className='flex justify-between border-b py-1'><span>{d.title}</span><span>{d.status}</span></div>)}</div></div>;}
